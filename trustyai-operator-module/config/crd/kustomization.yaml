apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CRD manifests are generated by controller-gen (make manifests-tom).
# Uncomment the line below once generated:
# resources:
#   - bases/components.platform.opendatahub.io_trustyais.yaml
