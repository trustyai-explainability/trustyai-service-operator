---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: guardrailsorchestrators.trustyai.opendatahub.io
spec:
  group: trustyai.opendatahub.io
  names:
    kind: GuardrailsOrchestrator
    listKind: GuardrailsOrchestratorList
    plural: guardrailsorchestrators
    singular: guardrailsorchestrator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GuardrailsOrchestrator is the Schema for the GuardrailsOrchestrator
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GuardrailsOrchestratorStatus defines the desired state of
              GuardrailsOrchestrator
            properties:
              chunker:
                description: Chunker name
                properties:
                  service:
                    properties:
                      hostname:
                        type: string
                      port:
                        type: integer
                      tls:
                        description: TLS credentials path
                        type: string
                    required:
                    - hostname
                    - port
                    - tls
                    type: object
                  type:
                    type: string
                required:
                - service
                - type
                type: object
              detectors:
                description: Detector name(s)
                items:
                  properties:
                    chunker_id:
                      description: Chunker ID
                      type: string
                    default_threshold:
                      type: string
                    service:
                      description: Service
                      properties:
                        hostname:
                          type: string
                        port:
                          type: integer
                        tls:
                          description: TLS credentials path
                          type: string
                      required:
                      - hostname
                      - port
                      - tls
                      type: object
                    type:
                      description: Detector name
                      type: string
                  required:
                  - chunker_id
                  - default_threshold
                  - service
                  - type
                  type: object
                type: array
              generator:
                description: Generator name
                properties:
                  provider:
                    description: Provider name
                    type: string
                  service:
                    description: Service
                    properties:
                      hostname:
                        type: string
                      port:
                        type: integer
                      tls:
                        description: TLS credentials path
                        type: string
                    required:
                    - hostname
                    - port
                    - tls
                    type: object
                required:
                - provider
                - service
                type: object
              replicas:
                description: Number of replicas
                format: int32
                type: integer
              tls:
                description: TLS
                properties:
                  ca_cert_path:
                    type: string
                  cert_path:
                    type: string
                  key_path:
                    type: string
                  type:
                    type: string
                required:
                - ca_cert_path
                - cert_path
                - key_path
                - type
                type: object
            required:
            - chunker
            - detectors
            - generator
            - replicas
            - tls
            type: object
          status:
            description: GuardrailsOrchestratorStatus defines the observed state of
              GuardrailsOrchestrator
            properties:
              conditions:
                items:
                  description: Condition defines the condition of the GuardrailsOrchestrator
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                type: string
              ready:
                type: string
              replicas:
                format: int32
                type: integer
            required:
            - conditions
            - phase
            - replicas
            type: object
        type: object
    served: true
    storage: true
