---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.3
  name: nemoguardrails.trustyai.opendatahub.io
spec:
  group: trustyai.opendatahub.io
  names:
    kind: NemoGuardrails
    listKind: NemoGuardrailsList
    plural: nemoguardrails
    singular: nemoguardrails
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NemoGuardrails is the Schema for the nemoguardrails API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NemoGuardrailsSpec defines the desired state of NemoGuardrails
            properties:
              nemoConfig:
                description: NemoConfig should be the name of the configmap containing
                  the NeMO server configuration
                type: string
            type: object
          status:
            description: NemoGuardrailStatus defines the observed state of NemoGuardrails
            properties:
              conditions:
                description: Conditions describes the state of the NemoGuardrails
                  resource.
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
