# YAML generated by controller-gen. DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clusterguardrailpolicies.trustyai.opendatahub.io
spec:
  group: trustyai.opendatahub.io
  names:
    kind: ClusterGuardrailPolicy
    listKind: ClusterGuardrailPolicyList
    plural: clusterguardrailpolicies
    singular: clusterguardrailpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetRef.kind
      name: TargetKind
      type: string
    - jsonPath: .spec.targetRef.name
      name: TargetName
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterGuardrailPolicy is the Schema for cluster-wide guardrail
          policy management.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterGuardrailPolicySpec defines the desired state of ClusterGuardrailPolicy.
            properties:
              defaults:
                description: Defaults defines guardrail rules that can be overridden
                  by namespace-level policies.
                properties:
                  guardrails:
                    description: Guardrails defines the default guardrail rules.
                    properties:
                      inputGuard:
                        description: InputGuard configures the input (request) guardrail
                          plugin.
                        properties:
                          enabled:
                            default: true
                            description: Enabled controls whether this guard plugin
                              is active.
                            type: boolean
                          nemoURL:
                            description: NemoURL overrides the auto-discovered NeMo
                              service URL.
                            type: string
                          timeoutSeconds:
                            default: 360
                            description: TimeoutSeconds for the NeMo guardrail check
                              call.
                            minimum: 1
                            type: integer
                        type: object
                      nemoGuardrails:
                        description: NemoGuardrails configures the NeMo Guardrails
                          server deployment.
                        properties:
                          env:
                            description: Env defines additional environment variables
                              for the NeMo container.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          nemoConfigs:
                            description: NemoConfigs lists the guardrail configuration
                              sets to deploy.
                            items:
                              description: NemoConfigRef references a ConfigMap containing
                                NeMo configuration.
                              properties:
                                configMaps:
                                  description: ConfigMaps lists ConfigMap names containing
                                    config files.
                                  items:
                                    type: string
                                  type: array
                                default:
                                  description: Default marks this config as the default
                                    for the NeMo server.
                                  type: boolean
                                name:
                                  description: Name of this config set (used as directory
                                    name in /app/config/).
                                  pattern: ^[a-zA-Z0-9_-]+$
                                  type: string
                              required:
                              - configMaps
                              - name
                              type: object
                            type: array
                          replicas:
                            default: 1
                            description: Replicas for the NeMo Guardrails deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        required:
                        - nemoConfigs
                        type: object
                      outputGuard:
                        description: OutputGuard configures the output (response)
                          guardrail plugin.
                        properties:
                          enabled:
                            default: true
                            description: Enabled controls whether this guard plugin
                              is active.
                            type: boolean
                          nemoURL:
                            description: NemoURL overrides the auto-discovered NeMo
                              service URL.
                            type: string
                          timeoutSeconds:
                            default: 360
                            description: TimeoutSeconds for the NeMo guardrail check
                              call.
                            minimum: 1
                            type: integer
                        type: object
                    type: object
                type: object
              guardrails:
                description: Guardrails defines the guardrail rules for this policy
                  (shorthand for atomic defaults).
                properties:
                  inputGuard:
                    description: InputGuard configures the input (request) guardrail
                      plugin.
                    properties:
                      enabled:
                        default: true
                        description: Enabled controls whether this guard plugin is
                          active.
                        type: boolean
                      nemoURL:
                        description: NemoURL overrides the auto-discovered NeMo service
                          URL.
                        type: string
                      timeoutSeconds:
                        default: 360
                        description: TimeoutSeconds for the NeMo guardrail check call.
                        minimum: 1
                        type: integer
                    type: object
                  nemoGuardrails:
                    description: NemoGuardrails configures the NeMo Guardrails server
                      deployment.
                    properties:
                      env:
                        description: Env defines additional environment variables
                          for the NeMo container.
                        items:
                          description: EnvVar represents an environment variable present
                            in a Container.
                          properties:
                            name:
                              description: |-
                                Name of the environment variable.
                                May consist of any printable ASCII characters except '='.
                              type: string
                            value:
                              description: |-
                                Variable references $(VAR_NAME) are expanded
                                using the previously defined environment variables in the container and
                                any service environment variables. If a variable cannot be resolved,
                                the reference in the input string will be unchanged. Double $$ are reduced
                                to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                Escaped references will never be expanded, regardless of whether the variable
                                exists or not.
                                Defaults to "".
                              type: string
                            valueFrom:
                              description: Source for the environment variable's value.
                                Cannot be used if value is not empty.
                              properties:
                                configMapKeyRef:
                                  description: Selects a key of a ConfigMap.
                                  properties:
                                    key:
                                      description: The key to select.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the ConfigMap or
                                        its key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fieldRef:
                                  description: |-
                                    Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                    spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                  properties:
                                    apiVersion:
                                      description: Version of the schema the FieldPath
                                        is written in terms of, defaults to "v1".
                                      type: string
                                    fieldPath:
                                      description: Path of the field to select in
                                        the specified API version.
                                      type: string
                                  required:
                                  - fieldPath
                                  type: object
                                  x-kubernetes-map-type: atomic
                                fileKeyRef:
                                  description: |-
                                    FileKeyRef selects a key of the env file.
                                    Requires the EnvFiles feature gate to be enabled.
                                  properties:
                                    key:
                                      description: |-
                                        The key within the env file. An invalid key will prevent the pod from starting.
                                        The keys defined within a source may consist of any printable ASCII characters except '='.
                                        During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                      type: string
                                    optional:
                                      default: false
                                      description: |-
                                        Specify whether the file or its key must be defined. If the file or key
                                        does not exist, then the env var is not published.
                                        If optional is set to true and the specified key does not exist,
                                        the environment variable will not be set in the Pod's containers.

                                        If optional is set to false and the specified key does not exist,
                                        an error will be returned during Pod creation.
                                      type: boolean
                                    path:
                                      description: |-
                                        The path within the volume from which to select the file.
                                        Must be relative and may not contain the '..' path or start with '..'.
                                      type: string
                                    volumeName:
                                      description: The name of the volume mount containing
                                        the env file.
                                      type: string
                                  required:
                                  - key
                                  - path
                                  - volumeName
                                  type: object
                                  x-kubernetes-map-type: atomic
                                resourceFieldRef:
                                  description: |-
                                    Selects a resource of the container: only resources limits and requests
                                    (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                  properties:
                                    containerName:
                                      description: 'Container name: required for volumes,
                                        optional for env vars'
                                      type: string
                                    divisor:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: Specifies the output format of
                                        the exposed resources, defaults to "1"
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    resource:
                                      description: 'Required: resource to select'
                                      type: string
                                  required:
                                  - resource
                                  type: object
                                  x-kubernetes-map-type: atomic
                                secretKeyRef:
                                  description: Selects a key of a secret in the pod's
                                    namespace
                                  properties:
                                    key:
                                      description: The key of the secret to select
                                        from.  Must be a valid secret key.
                                      type: string
                                    name:
                                      default: ""
                                      description: |-
                                        Name of the referent.
                                        This field is effectively required, but due to backwards compatibility is
                                        allowed to be empty. Instances of this type with an empty value here are
                                        almost certainly wrong.
                                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                      type: string
                                    optional:
                                      description: Specify whether the Secret or its
                                        key must be defined
                                      type: boolean
                                  required:
                                  - key
                                  type: object
                                  x-kubernetes-map-type: atomic
                              type: object
                          required:
                          - name
                          type: object
                        type: array
                      nemoConfigs:
                        description: NemoConfigs lists the guardrail configuration
                          sets to deploy.
                        items:
                          description: NemoConfigRef references a ConfigMap containing
                            NeMo configuration.
                          properties:
                            configMaps:
                              description: ConfigMaps lists ConfigMap names containing
                                config files.
                              items:
                                type: string
                              type: array
                            default:
                              description: Default marks this config as the default
                                for the NeMo server.
                              type: boolean
                            name:
                              description: Name of this config set (used as directory
                                name in /app/config/).
                              pattern: ^[a-zA-Z0-9_-]+$
                              type: string
                          required:
                          - configMaps
                          - name
                          type: object
                        type: array
                      replicas:
                        default: 1
                        description: Replicas for the NeMo Guardrails deployment.
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - nemoConfigs
                    type: object
                  outputGuard:
                    description: OutputGuard configures the output (response) guardrail
                      plugin.
                    properties:
                      enabled:
                        default: true
                        description: Enabled controls whether this guard plugin is
                          active.
                        type: boolean
                      nemoURL:
                        description: NemoURL overrides the auto-discovered NeMo service
                          URL.
                        type: string
                      timeoutSeconds:
                        default: 360
                        description: TimeoutSeconds for the NeMo guardrail check call.
                        minimum: 1
                        type: integer
                    type: object
                type: object
              overrides:
                description: Overrides defines guardrail rules that cannot be overridden
                  by namespace-level policies.
                properties:
                  guardrails:
                    description: Guardrails defines the override guardrail rules.
                    properties:
                      inputGuard:
                        description: InputGuard configures the input (request) guardrail
                          plugin.
                        properties:
                          enabled:
                            default: true
                            description: Enabled controls whether this guard plugin
                              is active.
                            type: boolean
                          nemoURL:
                            description: NemoURL overrides the auto-discovered NeMo
                              service URL.
                            type: string
                          timeoutSeconds:
                            default: 360
                            description: TimeoutSeconds for the NeMo guardrail check
                              call.
                            minimum: 1
                            type: integer
                        type: object
                      nemoGuardrails:
                        description: NemoGuardrails configures the NeMo Guardrails
                          server deployment.
                        properties:
                          env:
                            description: Env defines additional environment variables
                              for the NeMo container.
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          nemoConfigs:
                            description: NemoConfigs lists the guardrail configuration
                              sets to deploy.
                            items:
                              description: NemoConfigRef references a ConfigMap containing
                                NeMo configuration.
                              properties:
                                configMaps:
                                  description: ConfigMaps lists ConfigMap names containing
                                    config files.
                                  items:
                                    type: string
                                  type: array
                                default:
                                  description: Default marks this config as the default
                                    for the NeMo server.
                                  type: boolean
                                name:
                                  description: Name of this config set (used as directory
                                    name in /app/config/).
                                  pattern: ^[a-zA-Z0-9_-]+$
                                  type: string
                              required:
                              - configMaps
                              - name
                              type: object
                            type: array
                          replicas:
                            default: 1
                            description: Replicas for the NeMo Guardrails deployment.
                            format: int32
                            minimum: 1
                            type: integer
                        required:
                        - nemoConfigs
                        type: object
                      outputGuard:
                        description: OutputGuard configures the output (response)
                          guardrail plugin.
                        properties:
                          enabled:
                            default: true
                            description: Enabled controls whether this guard plugin
                              is active.
                            type: boolean
                          nemoURL:
                            description: NemoURL overrides the auto-discovered NeMo
                              service URL.
                            type: string
                          timeoutSeconds:
                            default: 360
                            description: TimeoutSeconds for the NeMo guardrail check
                              call.
                            minimum: 1
                            type: integer
                        type: object
                    type: object
                  strategy:
                    default: atomic
                    description: Strategy controls how overrides are applied. "atomic"
                      replaces the entire config; "merge" merges individual fields.
                    enum:
                    - atomic
                    - merge
                    type: string
                type: object
              targetNamespace:
                description: TargetNamespace is the namespace of the target Gateway.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: targetNamespace is immutable
                  rule: self == oldSelf
              targetRef:
                description: TargetRef identifies the Gateway to which this policy
                  applies.
                properties:
                  group:
                    default: gateway.networking.k8s.io
                    description: Group is the API group of the target resource.
                    type: string
                  kind:
                    default: Gateway
                    description: Kind is the kind of the target resource.
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    type: string
                required:
                - group
                - kind
                - name
                type: object
            required:
            - targetNamespace
            - targetRef
            type: object
            x-kubernetes-validations:
            - message: targetNamespace is immutable
              rule: self.targetNamespace == oldSelf.targetNamespace
          status:
            description: ClusterGuardrailPolicyStatus defines the observed state of
              ClusterGuardrailPolicy.
            properties:
              conditions:
                description: Conditions describes the state of the policy.
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              ippConfigRefs:
                description: IPPConfigRefs references the managed IPP plugin ConfigMaps.
                items:
                  description: ManagedResourceRef references a managed sub-resource.
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                    ready:
                      type: boolean
                  required:
                  - name
                  - namespace
                  - ready
                  type: object
                type: array
              nemoGuardrailsRef:
                description: NemoGuardrailsRef references the managed NemoGuardrails
                  CR.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                  ready:
                    type: boolean
                required:
                - name
                - namespace
                - ready
                type: object
              phase:
                description: Phase reflects the overall lifecycle state.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
